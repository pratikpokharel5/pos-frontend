export type ProductLineForm = {
  id: string;
  product_id: string;
  item_name: string;
  quantity: string;
  unit_price: string;
  discount_amount: string;
  notes: string;
};

export type NewCustomerForm = {
  name: string;
  phone: string;
};
